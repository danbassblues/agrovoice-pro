services:
  - type: web
    name: agrovoice-pro
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app_web:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0